spring:
    data:
        mongodb:
            username: root
            password: root
            host: localhost
            port: 27017
            database: writiq
            authentication-database: admin
            auto-index-creation: true
